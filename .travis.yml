
language : scala

scala:
  - 2.12.11
  - 2.13.0

dist: trusty 

cache:
  directories:
  - $HOME/.ivy2
  - $HOME/.sbt

jdk:
  - oraclejdk8

script:
  - sbt ++$TRAVIS_SCALA_VERSION -Dfile.encoding=UTF8 "project fs2-http" test


